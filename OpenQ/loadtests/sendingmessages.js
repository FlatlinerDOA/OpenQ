var http = require('http');
describe('When posting 100,000 messages to a single inbox, ', function () {
    it('then the average messages per second does not fall below 1,000', function () {
    });
});
